package New;
import java.time.Duration;

import org.openqa.selenium.By;
import org.openqa.selenium.Keys;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.edge.EdgeDriver;
import org.openqa.selenium.interactions.Actions;
import org.openqa.selenium.support.ui.ExpectedConditions;
import org.openqa.selenium.support.ui.Select;
import org.openqa.selenium.support.ui.WebDriverWait;

import io.github.bonigarcia.wdm.WebDriverManager;
public class Testinggggg {

	public static void main(String[] args) {
		WebDriverManager.edgedriver().setup();
		WebDriver driver =new EdgeDriver();
		driver.get("https://www.flipkart.com/");
		driver.manage().window().maximize();
		WebElement k=driver.findElement(By.xpath("//input[@class='Pke_EE']"));
		k.sendKeys("BlueTooth speakers");
k.sendKeys(Keys.ENTER);
	WebElement o=driver.findElement(By.xpath("//select[@class='_2YxCDZ']"));
	Select s= new Select(o);
	
	WebDriverWait wait=new WebDriverWait(driver,Duration.ofSeconds(20));
    wait.until(ExpectedConditions.visibilityOf(o));
    s.selectByVisibleText("800");
	
	
	WebElement o1=driver.findElement(By.xpath("(//select[@class='_2YxCDZ'])[2]"));
	Select s1=new Select(o1);
	WebDriverWait wait=new WebDriverWait(driver,Duration.ofSeconds(20));
    wait.until(ExpectedConditions.visibilityOf(o1));
	s1.selectByVisibleText("1300");
	}

}
